import React from "react";

import { Loader, LoaderWrapper } from "@core/styles/loaderStyles";

const AppLoader = () => {
    return (
        <LoaderWrapper>
            <Loader />
        </LoaderWrapper>
    );
};

export default AppLoader;
