import { CORE_PATH } from "@core/routing/routingConstants";

export const PROFILE_PAGE_PATH = `${CORE_PATH}profile`;
export const HISTORY_PAGE_PATH = `${CORE_PATH}history`;
export const RECORD_PAGE_PATH = `${CORE_PATH}record`;
export const CONTACTS_PAGE_PATH = `${CORE_PATH}contacts`;
